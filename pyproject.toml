[tool.poetry]
name = "cataloger-cli"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "cataloger_cli"}]

[tool.poetry.dependencies]
python = "^3.10"
fire = "^0.5.0"
selenium = "^4.7.2"
webdriver-manager = "^3.8.5"


[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"
pytest-cov = "^4.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
